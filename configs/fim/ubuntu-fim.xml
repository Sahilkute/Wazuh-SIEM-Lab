<!-- Ubuntu Desktop FIM Configuration -->
<!-- Add this inside <ossec_config> in ossec.conf on the Ubuntu agent -->

<syscheck>

  <!-- Enable FIM -->
  <disabled>no</disabled>

  <!-- Scan frequency in seconds (every 12 hours) -->
  <frequency>43200</frequency>

  <!-- Scan on startup -->
  <scan_on_start>yes</scan_on_start>

  <!-- Enable real-time monitoring with Whodata (audit) -->
  <directories realtime="yes" report_changes="yes" check_all="yes">/etc</directories>
  <directories whodata="yes" report_changes="yes" check_all="yes">/usr/bin</directories>
  <directories whodata="yes" report_changes="yes" check_all="yes">/usr/sbin</directories>
  <directories realtime="yes" report_changes="yes" check_all="yes">/home</directories>
  <directories realtime="yes" report_changes="yes" check_all="yes">/root</directories>
  <directories realtime="yes" report_changes="yes" check_all="yes">/tmp</directories>
  <directories realtime="yes" report_changes="yes" check_all="yes">/var/www</directories>

  <!-- Ignore these paths -->
  <ignore>/etc/mtab</ignore>
  <ignore>/etc/hosts.deny</ignore>
  <ignore>/etc/mail/statistics</ignore>
  <ignore>/etc/random-seed</ignore>
  <ignore>/etc/adjtime</ignore>
  <ignore>/etc/httpd/logs</ignore>
  <ignore>/etc/utmpx</ignore>
  <ignore>/etc/wtmpx</ignore>
  <ignore>/etc/cups/certs</ignore>
  <ignore>/etc/dumpdates</ignore>
  <ignore>/etc/svc/volatile</ignore>
  <ignore type="sregex">.log$|.tmp$|.swp$</ignore>

  <!-- Alert on new files -->
  <alert_new_files>yes</alert_new_files>

  <!-- Remove old diff files -->
  <remove_old_diff>yes</remove_old_diff>

  <!-- File size limit for report_changes -->
  <file_size>
    <limit>50</limit>
  </file_size>

</syscheck>
